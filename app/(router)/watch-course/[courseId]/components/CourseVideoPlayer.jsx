import React from 'react';

const CourseVideoPlayer = ({ lesson }) => {
  return (
    <div>
      <h2>{lesson.title}</h2>
      {/* Thêm logic để hiển thị video ở đây */}
    </div>
  );
};

export default CourseVideoPlayer;